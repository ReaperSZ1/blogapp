<div class="card mt-2">
        <div class="card-body">
            <h4>Titulo: {{postagem.titulo}}</h4>
            <small>{{postagem.conteudo}}</small>
            <br>
            <small>Categoria: {{postagem.categoria.nome}}</small>
            <br>
            <small>Data de Publicação: {{postagem.data}}</small>
        </div>
    </div>